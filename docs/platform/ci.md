## CI pipeline

The CI pipeline automates validation tasks defined by the project.

### Jobs
- Application tests
- Infrastructure lifecycle commands

CI executes the infrastructure lifecycle commands but does not deploy resources.
